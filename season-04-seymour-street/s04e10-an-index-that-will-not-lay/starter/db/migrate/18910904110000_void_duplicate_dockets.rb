# Шаг с данными: разобрать повторы.
#
# Правило Палаты: из строк с одной парой «бланк + участок» живой остаётся
# самая ранняя — внесённая первой. Остальные гасятся. Не удаляются: архив
# тем и архив, что из него не вычёркивают.
class VoidDuplicateDockets < ActiveRecord::Migration[8.0]
  # TODO: этот шаг идёт вне одной проводки.
  #
  #       Проводка на пять тысяч строк держит их все до конца; на ведомости в
  #       миллион строк она держит миллион, и всё это время в неё нельзя
  #       писать. Плата ровно одна: упавший посередине шаг оставляет половину
  #       работы сделанной — значит, он обязан переживать повторный запуск.

  # TODO: своя модель, не из `app/models`.
  #
  #       Миграцию прогонят и через пять лет, на пустой базе, при выкатке
  #       нового окружения. К тому времени настоящая модель обрастёт
  #       валидациями и связями, которых сейчас нет, а может исчезнуть вместе
  #       с таблицей.

  BATCH = 500

  def up
    # TODO: погасить все живые строки, кроме самой ранней в своей паре.
    #
    #       Порциями — не всё одним запросом и не строка за строкой.
    #
    #       Отбор обязан пересчитываться на каждом заходе: тогда уже
    #       погашенные строки в него не попадают, и шаг можно запускать
    #       сколько угодно раз.
  end

  # TODO: откат. Подумай, что тут вообще можно откатить: схему этот шаг не
  #       менял, а данные откатом не возвращаются.
end
