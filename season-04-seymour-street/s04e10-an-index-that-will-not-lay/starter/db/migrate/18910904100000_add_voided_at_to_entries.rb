# Отметка о погашении.
#
# Отдельный шаг, и только со схемой. Почему отдельный — станет видно на
# следующем.
class AddVoidedAtToEntries < ActiveRecord::Migration[8.0]
  def change
    # TODO: столбец voided_at — время; **необязательный**.
    #
    #       `null: false` здесь невозможен: строк уже пять тысяч четыреста, и
    #       ни у одной значения нет — база откажет на первой же.
  end
end
