# Теперь индекс ложится.
class EnforceOneLiveDocket < ActiveRecord::Migration[8.0]
  def change
    # TODO: уникальный индекс по паре «бланк + участок» — **с условием**:
    #       уникальность держится только на живой части архива. Погашенных
    #       строк с одной парой сколько угодно, живая одна.
    #
    # TODO: имя индексу дай сам. Rails придумает своё — и оно совпадёт с
    #       именем обычного индекса из первого шага.
  end
end
