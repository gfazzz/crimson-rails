# Отметка о погашении.
#
# Шаг только со схемой: столбец добавляется пустым и **необязательным**.
# `null: false` здесь невозможен — строк уже пять тысяч четыреста, и ни у
# одной значения нет; база откажет на первой же.
#
# Пустой столбец на живой таблице стоит дёшево. Дорого стоит следующий шаг,
# и потому он вынесен в отдельную миграцию.
class AddVoidedAtToEntries < ActiveRecord::Migration[8.0]
  def change
    add_column :entries, :voided_at, :datetime
  end
end
