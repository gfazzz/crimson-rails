# Перевозки. Здесь впервые появляются деньги, вес и дата — три вещи, которые
# в книге записаны словами, а в базе обязаны быть числом и датой.
#
# Тип столбца — это обещание, которое база даёт всем, кто в неё ходит. Не
# «подсказка для разработчика», а то, во что превращается всякое значение,
# прежде чем лечь.
class CreateConsignments < ActiveRecord::Migration[8.0]
  def change
    # TODO: таблица consignments — ведомость перевозок:
    #
    #   reference    номер бланка          строка, обязательный
    #   description  что везут             строка, обязательное
    #   sent_on      день отправки         дата — день, а не момент
    #   pence        плата                 целое число пенсов
    #   weight_lb    вес                   целое, в фунтах (единица — в имени)
    #   settled      рассчитана ли         да/нет, умолчание «нет» **в базе**
    #   + две даты, которые Rails ведёт сам
    #
    # Три решения, которые придётся принять осознанно:
    #
    #   почему номер бланка строка   над ним не считают, у него бывают буквы
    #                                и ведущие нули;
    #   почему плата целая           двоичная дробь не умеет представить ни
    #                                0.1, ни 0.7, и сумма тысячи плат
    #                                разойдётся с ручным счётом — всегда в
    #                                одну сторону;
    #   почему умолчание в базе      строка, пришедшая мимо модели, тоже
    #                                обязана иметь ответ. Иначе третьим
    #                                состоянием становится NULL.
  end
end
