# Дорога — член Расчётной палаты.
#
# Модель — это не «таблица в коде». Схема уже объявила форму и держит её для
# всех; модель добавляет то, чего схема не умеет: объясняет человеку, что не
# так, и приводит введённое к общему виду.
#
# Всё, что здесь написано, действует только для того, кто пришёл через модель.
# Это не недостаток — это её работа.
class Company < ApplicationRecord
  # TODO: привести код дороги к общему виду до проверки — без пробелов по
  #       краям, заглавными. Код пишут как попало («mid», « MID », «Mid»),
  #       а в реестре он один. Rails 7.1+ умеет это одной строкой:
  #       `normalizes :поле, with: ->(значение) { ... }`.

  # TODO: название обязательно.
  # TODO: код обязателен и длиной от двух до четырёх знаков.
  #       Заметь: в схеме такого правила нет и не будет — длину SQLite не
  #       проверяет. Это правило живёт только в модели, и проверка серии
  #       показывает, что из этого следует.
end
