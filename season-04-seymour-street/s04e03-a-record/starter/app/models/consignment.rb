# Перевозка.
#
# Валидации здесь делают две вещи, которых схема делать не умеет: называют
# поле, которое не понравилось, и объясняют почему. Ни одна из них не защищает
# базу — за каждой обязано стоять ограничение, и в s04e08 они появятся.
class Consignment < ApplicationRecord
  # TODO: привести номер бланка к общему виду — как код дороги.

  # TODO: номер бланка, груз и день отправки обязательны.
  # TODO: плата — целое число больше нуля.
  # TODO: вес — целое число больше нуля.
  #
  #       `numericality` без `allow_nil` отвергает и пустое значение: «не
  #       число». Отдельная `presence` рядом с ней — вторая жалоба об одном и
  #       том же.
end
