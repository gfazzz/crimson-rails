# Условия, которые проверяет база.
#
# До сих пор схема отвечала на вопрос «какие графы бывают». Здесь она начинает
# отвечать на вопрос «какие значения бывают» — и это те же самые правила,
# которые уже стоят в моделях с третьей серии. Разница прежняя: модель просит,
# база отказывает.
class AddConditionsToTheLedger < ActiveRecord::Migration[8.0]
  def change
    # TODO: столбец role у участка — целое, обязательное, с умолчанием.
    #
    #       Роль хранится числом: у неё есть порядок, и число занимает меньше
    #       места, чем строка, повторённая миллион раз. Ролей три: принял,
    #       вёз, сдал.
    #
    #       Умолчание выбери сам — но подумай, какая из трёх ролей бывает у
    #       любого числа участков, а какие по одной на перевозку. Умолчание,
    #       которое нарушается самим фактом второй строки, — плохое умолчание.

    # TODO: условие, что роль бывает только одной из трёх.
    #       Без него `enum` остаётся договорённостью внутри приложения.
    #       Условию нужно имя: безымянное база назовёт сама, и в отказе
    #       окажется что-нибудь вроде chk_rails_8f3a2c.

    # TODO: принявший участок в перевозке ровно один.
    #
    #       Обычный уникальный индекс по consignment_id запретил бы вообще
    #       вторую строку — а везущих участков бывает сколько угодно.
    #       Уникальность нужна не всегда, а **при условии**: это умеет
    #       частичный индекс (`where:`).

    # TODO: суммы, которых не бывает, — те же правила, что в моделях с
    #       s04e03, но теперь как условия базы:
    #         плата больше нуля;
    #         вес больше нуля;
    #         мили участка больше нуля.
  end
end
