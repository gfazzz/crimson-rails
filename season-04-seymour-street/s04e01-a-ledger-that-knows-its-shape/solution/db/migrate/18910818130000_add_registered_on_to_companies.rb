# Вторая миграция — и это главное, что в ней есть.
#
# Столбец понадобился через час после первой. Соблазн — открыть первую
# миграцию и дописать строку: файл ещё тёплый, база одна, никто не заметит.
# Не заметит ровно до того дня, когда рядом появится второй человек: у него
# первая миграция уже прогнана, её версия записана, и дописанная строка до
# него не доедет никогда.
#
# Схема растёт слоями. Слой, который уже лёг, не переписывают.
class AddRegisteredOnToCompanies < ActiveRecord::Migration[8.0]
  def change
    # Без `null: false`: дороги вступали в Палату с 1842 года, и у половины
    # членов даты вступления в книге просто нет. Обязательность, которой
    # нельзя выполнить, — это не строгость, а невозможность завести запись.
    add_column :companies, :registered_on, :date
  end
end
