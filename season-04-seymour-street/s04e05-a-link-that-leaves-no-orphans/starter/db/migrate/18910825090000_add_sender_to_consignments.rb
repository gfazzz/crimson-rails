# У перевозки появляется отправитель — дорога, принявшая груз.
#
# Связь в Rails живёт в двух местах, и путать их нельзя:
#
#   belongs_to / has_many   в модели: как одно найти по другому;
#   внешний ключ            в схеме: что ссылка ведёт на существующую строку.
#
# Первое — удобство. Второе — единственное, что мешает ведомости завести
# перевозку с дороги, которой нет.
class AddSenderToConsignments < ActiveRecord::Migration[8.0]
  def change
    # TODO: добавить в consignments ссылку на дорогу-отправителя.
    #
    #       Нужны три вещи, и `add_reference` умеет все три разом:
    #         столбец company_id;
    #         индекс по нему — иначе «все перевозки этой дороги» читают всю
    #           ведомость, а удаление дороги тем более;
    #         внешний ключ — иначе ссылка на дорогу это просто число.
    #
    #       И `null: false`: перевозки без отправителя не бывает. Это не то же
    #       самое, что внешний ключ — ключ следит, чтобы ссылка вела **на
    #       существующую** строку, а `null: false` чтобы ссылка вообще была.
  end
end
