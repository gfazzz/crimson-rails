# Расчёт: сколько Палата должна дороге за месяц.
#
# Первая ведомость сезона, которую не вводят с бланков, а **считают**. Отсюда
# и тема серии: счёт, который идёт по нескольким строкам, обязан лечь целиком
# или не лечь вовсе.
class CreateSettlements < ActiveRecord::Migration[8.0]
  def change
    # TODO: таблица settlements:
    #
    #   company      ссылка на дорогу: обязательная, с ключом и индексом
    #   period       месяц, «1891-08». Строка, потому что это не дата, а
    #                название месяца: сравнивать его будут на равенство
    #   pence        сумма, целое, обязательное
    #   state        посчитан или оплачен; число, обязательное, с умолчанием
    #   lock_version счётчик правок. Его ведёт Rails сам: при каждой записи
    #                он растёт, а при записи из устаревшей копии не совпадает —
    #                и запись отвергается. Имя не произвольное: по нему Rails
    #                и узнаёт столбец
    #   + две даты
    #
    # TODO: один расчёт на дорогу за месяц. То же правило, что в s04e04, и та
    #       же причина: пересчёт, начатый дважды, не должен дать две строки.
    #
    # TODO: условия — сумма больше нуля, состояние из двух известных. С именами.
  end
end
