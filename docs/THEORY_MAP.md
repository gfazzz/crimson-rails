# Карта концептов

Где концепт **вводится** и где после этого **используется**. Правило курса
(`SPEC.md` §11.7): в зачётном коде серии не может встретиться то, что ещё не
введено. Эта таблица — способ проверить правило, а не украшение.

## Season 1 — Ruby

| Концепт | Введён | Используется дальше |
|---|---|---|
| блок, `yield`, `block_given?` | s01e01 | везде |
| возврат `self` из обхода | s01e01 | e02, e03, e10 |
| истинность (`nil`/`false` ложны) | s01e01 | e08, e13 |
| `ArgumentError` со списком допустимого | s01e01 | e02, e06, e09, e13 |
| блок как объект, `&`, `Proc`/`lambda` | s01e02 | e03, e06, e10, e13 |
| замыкание | s01e02 | e10, e13, e15 |
| утиная типизация (`respond_to?`) | s01e02 | e09, e12, e15 |
| `Hash#fetch` с блоком и умолчанием | s01e02 | e05, e06, e09, e10 |
| `include`, цепочка предков | s01e03 | e09, e15 |
| `Enumerable`, контракт `each` | s01e03 | e04, e05, e10 |
| `Enumerator`, `enum_for`, `lazy` | s01e03 | e10 |
| `alias` | s01e03 | — |
| `map` / `select` / `reject` / `partition` | s01e04 | e05, e12 |
| `reduce` с начальным значением | s01e04 | e05 |
| `nil` как честный ответ | s01e04 | e11 |
| `group_by`, `transform_values`, `tally` | s01e05 | e13 |
| `each_with_object` | s01e05 | e07, e11 |
| сверка по объединению ключей | s01e05 | — |
| именованные аргументы, `**` | s01e06 | e08, e09, e13 |
| отказ от неизвестного ключа | s01e06 | e10, e13 |
| `freeze`, неизменяемость | s01e06 | e08 |
| символ против строки | s01e07 | e08, e13 |
| рекурсивное приведение ключей | s01e07 | e08 |
| объект-значение, `==`/`eql?`/`hash` | s01e08 | e15 (приёмка) |
| `to_s` против `inspect` | s01e08 | — |
| модуль как роль; `extend`, `prepend` | s01e09 | e10, e13, e15 |
| хук `included` | s01e09 | e13, e15 |
| `super` из примеси | s01e09 | e12, e15 |
| четыре контекста `self` | s01e10 | e13, e15 |
| переменные объекта-класса | s01e10 | e13, e15 |
| `class << self` | s01e10 | e13 |
| `instance_eval` | s01e10 | e15 |
| иерархия исключений от `StandardError` | s01e11 | e12 (`NoMethodError`), e15 |
| `ensure`, `retry` с пределом | s01e11 | — |
| контекст внутри исключения | s01e11 | — |
| `method_missing`, `respond_to_missing?` | s01e12 | e13 (сравнение), e15 |
| `define_method` | s01e13 | e15 |
| макрос уровня класса | s01e13 | e15 |
| хук `inherited` | s01e13 | — |
| `send` против `public_send` | s01e13 | — |
| гем, `require`, `$LOAD_PATH` | s01e14 | e15 |
| `append_features`, отложенные зависимости | s01e15 | — |

## Season 2 — HTML и CSS

| Концепт | Введён | Используется дальше |
|---|---|---|
| тег описывает содержание, а не вид | s02e01 | везде |
| ориентиры страницы (`header`/`nav`/`main`/`footer`) | s02e01 | e04, e08, e12 |
| один `<h1>`, уровни без пропусков | s02e01 | везде |
| `<time datetime>`, `<abbr title>` | s02e01 | e12 |
| сноски как список с `id` | s02e01 | e02, e12 |
| `<caption>`, `<thead>`/`<tbody>` | s02e02 | e08, e10, e12 |
| `th scope="col"`, `scope="row"`, `colgroup` | s02e02 | e04, e08, e12 |
| одинаковая ширина строк таблицы | s02e02 | e12 |
| `<label for>`, обёртка, `aria-label` | s02e03 | e04, e10 |
| типы полей и встроенная проверка | s02e03 | — |
| `fieldset` + `legend`, группа переключателей | s02e03 | — |
| `get` против `post` | s02e03 | сезон 4 |
| `aria-describedby` | s02e03 | e04 |
| первое правило ARIA | s02e04 | e08, e10, e12 |
| ссылка в обход оглавления | s02e04 | e06, e07, e08, e12 |
| три случая `alt` | s02e04 | e12 |
| `<figure>` и `<figcaption>` | s02e04 | e10, e12 |
| `lang` у вкрапления | s02e04 | e12 |
| порядок обхода, запрет на `tabindex` > 0 | s02e04 | e07, e10 |
| `aria-current` | s02e04 | e12 |
| каскад: происхождение, важность, вес, порядок | s02e05 | e06–e12 |
| подсчёт специфичности | s02e05 | e06–e12 |
| `:where()` и нулевой вес | s02e05 | e06–e11 |
| наследование против каскада | s02e05 | e09, e11 |
| свод от общего к частному | s02e05 | e06–e12 |
| блочная модель, `border-box` | s02e06 | e07, e08 |
| схлопывание полей и его прекращение | s02e06 | e07 |
| логические стороны (`block`/`inline`) | s02e06 | e07–e11 |
| сокрытие без потери доступности | s02e06 | e07, e08, e12 |
| Flexbox: ряд, оси, перенос | s02e07 | e08 (сравнение), e10 |
| `gap` вместо полей | s02e07 | e08 |
| `flex` по частям (`grow`/`shrink`/`basis`) | s02e07 | — |
| запрет на перестановку порядка раскладкой | s02e07 | e08 |
| Grid: области с именами | s02e08 | e10, e12 |
| `minmax(0, 1fr)`, `fit-content()` | s02e08 | e10 |
| выравнивание (`align-items: start`) | s02e08 | e10 |
| `auto-fit` / `auto-fill` | s02e08 | — |
| `rem` против `em`, кегль читателя | s02e09 | e10, e11 |
| мера строки в `ch` | s02e09 | e12 |
| шкала на пользовательских свойствах | s02e09 | e11, e12 |
| `clamp()` со слагаемым от кегля | s02e09 | e10, e12 |
| объявление области просмотра | s02e09 | e10, e12 |
| медиазапрос, «сперва узкое» | s02e10 | e11, e12 |
| перелом по содержимому и в `em` | s02e10 | e11, e12 |
| печатный набор | s02e10 | e11, e12 |
| доступная прокручиваемая область | s02e10 | e12 |
| контейнерные запросы (упомянуты) | s02e10 | — |
| палитра по ролям | s02e11 | e12 |
| `prefers-color-scheme`, `color-scheme` | s02e11 | e12 |
| коэффициент контраста и пороги | s02e11 | e12 |
| цвет — не единственный признак | s02e11 | e12 |
| ERB: три скобки, обрезка | s02e12 | сезон 5 |
| экранирование и `html_safe` | s02e12 | сезон 5 |
| данные отдельно от шаблона | s02e12 | сезоны 4–8 |
| проверка шаблона сменой данных | s02e12 | сезоны 4–8 |

## Season 3 — JavaScript

| Концепт | Введён | Используется дальше |
|---|---|---|
| скрипт улучшает то, что уже работает | s03e01 | везде, проверяется в e10 |
| модуль ES: `type="module"`, `import`/`export`, отложенность | s03e01 | везде |
| `dataset` и атрибут как договор с разметкой | s03e01 | e02, e05, e07, e09 |
| `hidden` против удаления узла | s03e01 | e03, e05, e09 |
| дерево документа: узлы, `querySelector`, обход | s03e02 | везде |
| `textContent` против `innerHTML` | s03e02 | e08 |
| создание и вставка узлов | s03e02 | e05, e08 |
| событие, объект события, всплытие | s03e03 | e04, e05, e09, e10 |
| делегирование и `closest` | s03e03 | e05, e08, e10 |
| `preventDefault` и когда его нельзя звать | s03e03 | e04, e07, e10 |
| слушатель однажды: пометка в `dataset` | s03e03 | e05, e07 |
| отправка формы, `submit`, `FormData` | s03e04 | e07, e10 |
| `checkValidity`, `setCustomValidity`, `noValidate` | s03e04 | e07 |
| живая область (`role="status"`) и фокус в поле | s03e04 | e07, e09 |
| состояние отдельно от отрисовки | s03e05 | e06–e10 |
| неизменяемое обновление состояния | s03e05 | e06–e10 |
| отрисовка приводит дерево к состоянию | s03e05 | e06–e10 |
| обещания, `async`/`await`, `Promise.allSettled` | s03e06 | e07, e09 |
| своя ошибка с `cause`; срок ожидания | s03e06 | e07 |
| `fetch`, коды ответа, разбор тела | s03e07 | e10 |
| `AbortController` и отмена запроса | s03e07 | — |
| повтор с выдержкой; ключ приёма против задвоения | s03e07 | сезон 6 |
| журнал из источника: состояние заменяется целиком | s03e07 | e08–e10 |
| `<template>` и `cloneNode` | s03e08 | e09 |
| чужой текст остаётся текстом | s03e08 | e09, e10 |
| список разрешённого; `new URL` как разбор | s03e08 | e10 |
| объявленная форма записи вместо `{ ...item }` | s03e08 | e09, e10 |
| `customElements.define`, подъём тега | s03e09 | e10 |
| `connectedCallback` / `disconnectedCallback` | s03e09 | сезон 5 |
| `observedAttributes`, `attributeChangedCallback` | s03e09 | сезон 5 |
| фокус, который не теряется при отрисовке | s03e09 | сезон 5 |
| `Intl.RelativeTimeFormat` | s03e09 | — |
| `URLSearchParams`, адрес как состояние | s03e10 | сезоны 4–5 |
| `pushState` / `replaceState` / `popstate` | s03e10 | сезон 5 |
| вид, данные и местное — три части состояния | s03e10 | сезоны 4–8 |

## Season 4 — схема и Active Record

| Концепт | Введён | Используется дальше |
|---|---|---|
| миграция, версия, `db/schema.rb` | s04e01 | везде, проверяется в e10 |
| `create_table`, `t.<тип>`, `change` и обратимость | s04e01 | e02, e05, e06, e10 |
| `rails db:migrate` / `db:migrate:reset` / `db:prepare` | s04e01 | везде |
| обязательность столбца (`null: false`) | s04e01 | e02, e05, e10 |
| типы: целое, строка, текст, дата, момент, логическое | s04e02 | e05–e10 |
| деньги целым числом наименьшей единицы | s04e02 | e06, e07, e09 |
| умолчание в схеме против умолчания в модели | s04e02 | e08, e10 |
| `null` как «неизвестно» | s04e02 | e08, e10 |
| `new` / `save` / `save!` / `create!`; `valid?`, `errors` | s04e03 | e04–e09 |
| валидация — просьба, ограничение — отказ | s04e03 | весь сезон, проверяется в e10 |
| `normalizes`, `changed?`, `_was` | s04e03 | e07, e09 |
| `validates :uniqueness` и окно между чтением и записью | s04e04 | e06, e08, e09 |
| уникальный индекс; `RecordNotUnique` | s04e04 | e06, e08, e09, e10 |
| составной индекс и порядок столбцов в нём | s04e04 | e06, e09, e10 |
| `belongs_to` / `has_many`; `inverse_of` | s04e05 | e06–e09 |
| внешний ключ в схеме; `dependent:` и выбор между вариантами | s04e05 | e06, e07, e09 |
| `has_many :through`; своя таблица связи | s04e06 | e07, e09 |
| составной уникальный ключ на связи | s04e06 | e08, e10 |
| `where`, `order`, `limit`; отбор (`scope`) как кусок запроса | s04e07 | e08, e09 |
| ленивость отношения; когда запрос уходит в базу | s04e07 | e09 |
| N+1 и `includes`; `group(...).sum` вместо счёта в Ruby | s04e07 | e09 |
| `CHECK` с именем | s04e08 | e09, e10 |
| частичный индекс (`where:`) | s04e08 | e10 |
| `enum` и чего он не даёт | s04e08 | e09 |
| выбор умолчания: оно не нарушается второй строкой | s04e08 | e10 |
| `transaction`, откат, `ActiveRecord::Rollback` | s04e09 | e10, сезоны 5–8 |
| идемпотентность: `find_or_initialize_by` за уникальным индексом | s04e09 | сезоны 6–7 |
| `with_lock`, `lock_version`; две блокировки и цена каждой | s04e09 | сезоны 6–7 |
| три шага миграции: схема, данные, ограничение | s04e10 | сезоны 5–8 |
| столбец на непустую таблицу | s04e10 | сезоны 5–8 |
| `in_batches`; проход мимо моделей приложения | s04e10 | сезон 6 |
| `disable_ddl_transaction!` и перезапускаемый шаг | s04e10 | сезон 8 |
| ограничение задним числом; `validate: false` | s04e10 | сезон 8 |

## Season 5 — маршруты, контроллеры, Hotwire

| Концепт | Введён | Используется дальше |
|---|---|---|
| маршрут → действие контроллера; `resources`, `only:` | s05e01 | весь сезон, проверяется в e11 |
| `rails routes`; помощники `*_path` и `*_url` | s05e01 | весь сезон |
| 404 там, где записи нет; `find` против `find_by` | s05e01 | e02, e06, e11 |
| параметр маршрута; `to_param`, `param:` | s05e02 | e04–e11 |
| ограничение на параметр (`constraints`) | s05e02 | e06, e11 |
| `before_action`: одно решение в одном месте | s05e02 | e04–e11 |
| макет, `yield`, частичные шаблоны, помощники | s05e03 | e04–e11 |
| экранирование по умолчанию в ERB | s05e03 | e04, e09 |
| разметка сезона 2 в шаблонах: язык, `main`, заголовки, таблица | s05e03 | проверяется в e11 |
| `form_with`, модель в форме, ошибки у поля | s05e04 | e05, e08–e10 |
| сильные параметры (`params.expect`) | s05e04 | e05, e08, e09 |
| 303 после удачной формы, 422 после неудачной | s05e04 | e05, e08, e09 |
| отказ базы (`RecordNotUnique`) словами на форме | s05e04 | e09 |
| `edit` / `update` / `destroy`; 303 после `DELETE` | s05e05 | e08 |
| отказ с причиной (`restrict_with_error`) | s05e05 | e09 |
| вложенные маршруты; `shallow` и почему не здесь | s05e06 | e08, e09, e11 |
| поиск в пределах родителя: чужая запись — 404 | s05e06 | e08, e09, e11 |
| Turbo Drive: визит без перезагрузки, снимок, кеш | s05e07 | e08–e10 |
| `data-turbo-permanent`, `data-turbo-temporary` | s05e07 | e09 |
| Turbo Frame: кусок со своим адресом; заголовок `Turbo-Frame` | s05e08 | e09, e11 |
| ленивый фрейм (`loading: :lazy`); «Content missing» | s05e08 | проверяется в e11 |
| правка на месте; `_top` | s05e08 | e09 |
| Turbo Stream: `replace`, `append`, `update`; тип ответа | s05e09 | сезон 6 |
| цель потока, которая есть на странице | s05e09 | сезон 6 |
| ответ без Turbo: тот же адрес, обычная страница | s05e09 | e11 |
| Stimulus: контроллер, цели, значения, действия | s05e10 | — |
| `connect` после визита Turbo; граница «где Turbo мало» | s05e10 | — |
| `respond_to`, `request.format`; расширение, параметр, `Accept` | s05e11 | сезоны 6–7 |
| Jbuilder; `*_url` в ответе; деньги целыми пенсами | s05e11 | сезон 6 |
| `send_data`, `Content-Disposition`; `download` мимо Turbo | s05e11 | — |
| 406; `rescue_from` — отказ в формате вопроса | s05e11 | сезоны 6–7 |
| приёмка обходом таблицы маршрутов | s05e11 | сезон 8 |

## Season 6 — фон, кеш, каналы, интеграции

| Концепт | Введён | Используется дальше |
|---|---|---|
| Active Job; `perform_later` против `perform_now` | s06e01 | весь сезон |
| аргументы задачи: GlobalID, а не копия полей | s06e01 | e02–e11 |
| исходы задачи; `discard_on ActiveJob::DeserializationError` | s06e01 | e03, e04 |
| честное состояние после постановки в очередь | s06e01 | e05 |
| Solid Queue: готовые, отложенные, взятые, упавшие — таблицы | s06e02 | весь сезон, проверяется в e11 |
| работник, диспетчер, надзиратель; возврат записок умершего | s06e02 | e04, e11 |
| `queue_as`, `queue_with_priority`, `config/queue.yml` | s06e02 | e04, e09 |
| `set(wait_until:)`; часы станции в поясе приложения | s06e02 | e10 |
| иерархия ошибок клиента: обрыв, отказ, своя | s06e03 | e05, e11 |
| `retry_on`: растущая пауза, разброс, предел, блок исчерпания | s06e03 | e05, e11 |
| `discard_on` с блоком; упавшие как список того, что чинить | s06e03 | e06, e11 |
| «хотя бы раз» и «ровно раз»; идемпотентная задача | s06e04 | весь сезон, проверяется в e11 |
| ключ эффекта — вещь, а не конверт | s06e04 | e06, e10, e11 |
| уникальный индекс вместо «а нет ли уже?»; `create_or_find_by!` | s06e04 | e06, e10, e11 |
| эффекты в одной транзакции с запиской (transactional outbox) | s06e04 | e11 |
| пределы ожидания `Net::HTTP`; молчание как обрыв; `max_retries: 0` | s06e05 | e11 |
| `Idempotency-Key`: ключ повтора для чужой стороны | s06e05 | e11 |
| «не знаю» — значение, а не исключение | s06e05 | e11 |
| чужой вызов — вне транзакции | s06e05 | e11 |
| входящий вызов: HMAC над сырым телом и временем, `secure_compare` | s06e06 | e11, сезон 7 |
| `skip_forgery_protection`; 202, 401, 422 чужой системе | s06e06 | e11 |
| `Rails.cache`, Solid Cache; ключ с версией книг | s06e07 | e08, e11 |
| `cache_key_with_version`: число строк и последняя правка | s06e07 | e08 |
| копия куска страницы; `touch: true`; матрёшка | s06e07 | e09 |
| условный запрос: `ETag`, `Last-Modified`, `304`; `fresh_when` | s06e08 | e11 |
| `Rack::ETag` и почему его мало; `Cache-Control`, эвристическая свежесть | s06e08 | сезон 8 |
| `turbo_stream_from`; подписанное имя потока | s06e09 | e11, сезон 7 |
| `broadcast_*_later_to` после коммита; `touch` как трансляция | s06e09 | e11 |
| адаптеры Action Cable; Solid Cable между процессами | s06e09 | сезон 8 |
| `config/recurring.yml`; одна записка на минуту расписания | s06e10 | e11 |
| сутки по поясу конторы; «вчера» от `enqueued_at` | s06e10 | e11 |
| два календаря: `Date::JULIAN`, `#gregorian` | s06e10 | e11 |
| `upsert` / `upsert_all` с `unique_by` | s06e10 | e11 |
| сверка с чужой книгой; «своя» строка по ключу | s06e11 | сезон 8 |
| приёмка сценарием под нагрузкой, с зерном | s06e11 | сезон 8 |

## Как проверять

1. Открыть `solution/` и `tests/` серии.
2. Выписать всё, что используется.
3. Сверить с таблицей: каждая строка должна быть введена в этой серии или
   раньше.

Концепт, используемый раньше, чем введён, — это либо повод перенести серию,
либо повод объяснить концепт на месте. Молча оставлять нельзя: студент,
идущий подряд, споткнётся именно здесь.

## Что намеренно не вошло

**Сезон 1.** `Fiber`, `Ractor`, потоки, `refinements`, `ObjectSpace`,
C-расширения, сокеты, работа с файлами, построение своего DSL с нуля,
`Struct`/`Data` как основной инструмент (упомянуты в теории s01e08), `curry`
(упомянут в теории s01e02).

**Сезон 2.** Препроцессоры, сборщики, CSS-фреймворки, методологии именования
(BEM и родня), `float`-раскладка, анимации и переходы, `@layer` (упомянут в
теории s02e05), контейнерные запросы (упомянуты в теории s02e10), `subgrid`
(упомянут в теории s02e08), веб-шрифты, SVG-графика, `<canvas>`, веб-компоненты.

**Сезон 3.** Сборщики и транспиляторы, TypeScript, любой фреймворк, `class` как
основа кода (нужен ровно один раз — для своего элемента), прототипы и `this`
в глубину, теневое дерево (упомянуто в теории s03e09), `IntersectionObserver`
и `ResizeObserver`, WebSocket и Server-Sent Events (сезон 6), сервис-воркеры,
`localStorage` (упомянут как анти-паттерн в s03e10), `canvas`, анимации,
тестовые раннеры сторонних поставщиков.

**Сезон 4.** Миграции с `up`/`down` как основной стиль (`change` покрывает почти
всё), `has_one` и полиморфные связи, однотабличное наследование, `counter_cache`,
`touch:`, `delegated_type`, `composed_of`, области видимости по умолчанию
(`default_scope` — упомянут в теории s04e10 как то, чего не делают), `find_by_sql`
и Arel, сырые представления и материализованные, триггеры и хранимые процедуры
(упомянуты в теории s04e08), разделение таблиц, несколько баз и реплики,
`ActiveRecord::Encryption`, фабрики и фикстуры Rails (проверки курса собирают
строки сами), поиск по тексту.

**Сезон 5.** Генераторы (`scaffold` и родня), `shallow`-маршруты (упомянуты в
теории s05e06), `concerns` в маршрутах, `ActionController::API` и токены
(сезон 7), сессии, куки и вход (сезон 7), `Turbo::Broadcastable` и потоки по
каналу (сезон 6), `morph` и обновление страницы на месте (упомянуто в теории
s05e07), Stimulus глубже одной серии: выходы (`outlets`), свои события,
несколько контроллеров, сборщики и `importmap` сверх того, что даёт Rails,
Action Text и Active Storage, версии API (сезон 6), кеширование ответа
(`ETag`, `fresh_when` — сезон 6), системные тесты Rails (`ApplicationSystemTestCase`
— проверки курса поднимают браузер сами).

**Сезон 6.** Redis, Sidekiq, GoodJob и прочие адаптеры очередей (упомянуты
в теории s06e02), `limits_concurrency` и уникальные задачи Solid Queue
(упомянуты в s06e02, s06e04 как то, что повторов не отменяет), Mission
Control — Jobs (упомянут в s06e03), автоматический выключатель и `Retry-After`
(упомянуты в s06e03, s06e05), Faraday и HTTP.rb (упомянуты в s06e05), смена
секрета вебхука и асимметричная подпись (сезон 7), `ActionController::API`
(упомянут в s06e06), `race_condition_ttl` и `counter_cache` (упомянуты в
s06e07), `Vary`, CDN и `s-maxage` (упомянуты в s06e08), свои каналы Action
Cable и `broadcast_refresh` с morph (упомянуты в s06e09), догон пропущенных
задач расписания и летнее время (упомянуты в s06e10), Action Mailer,
Active Storage, распределённые транзакции.

Критерий отбора — в `CONCEPT.md`: концепт входит в сезон, только если без него
Rails выглядит магией.
