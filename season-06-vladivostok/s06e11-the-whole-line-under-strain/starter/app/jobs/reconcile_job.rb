# Сверка книги выплат конторы с книгой казначейства за месяц.
#
#   ReconcileJob.perform_later("1892-05")   — за этот месяц
#   ReconcileJob.perform_later              — за прошлый (от постановки)
#
# Переплата (Overpayment: поставка, квитанция казначейства, месяц, сумма) —
# всё, что казна выплатила за поставку сверх одного раза. «Своя» выплата —
# по той же квитанции, по которой выплатила контора; если контора ещё не
# платила — первая по книге казначейства.
class ReconcileJob < ApplicationJob
  def perform(month = nil)
    # TODO
  end
end
