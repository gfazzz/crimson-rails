# Таблица маршрутов окна в реестр.
#
# Маршрут — обещание: запрос по этому адресу дойдёт до этого решения.
Rails.application.routes.draw do
  # Проверка живости для выката: отвечает 200, если приложение поднялось.
  get "up" => "rails/health#show", as: :rails_health_check

  # TODO: реестр дорог — список и одна дорога. Ровно это и ничего сверх:
  #       заводить, править и убирать дороги окно пока не умеет. Маршрут, за
  #       которым нет действия, — обещание, которое не выполнят.
  #
  #       Проверь себя: `bin/rails routes -c companies` должен показать две
  #       строки, а не семь.

  # TODO: корень — тот же реестр: с него контора начинает.
end
