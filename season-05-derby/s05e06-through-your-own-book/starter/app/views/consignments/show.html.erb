<% content_for :title, @consignment.reference %>

<h1>Бланк <%= @consignment.reference %></h1>

<%# TODO: отправитель (ссылкой на его страницу), дата, груз, плата, вес.

    TODO: участки — таблицей с подписью «Участки перевозки …»: номер участка
          заголовком строки, дорога ссылкой на её страницу, роль словом,
          мили. Строка участка — частичный шаблон legs/_leg. %>

<p><%= link_to "Книга перевозок #{@company.code}", company_consignments_path(@company) %></p>
