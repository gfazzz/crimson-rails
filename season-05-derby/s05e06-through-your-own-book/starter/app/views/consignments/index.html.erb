<% content_for :title, "Книга перевозок #{@company.code}" %>

<h1>Книга перевозок <%= @company.code %></h1>

<%# TODO: перевозки дороги — таблицей с подписью: номер бланка (заголовком
          строки и ссылкой на страницу перевозки в этой книге), дата
          отправки, груз, плата фунтами, шиллингами и пенсами.

          Пустая книга — фраза, не пустая таблица. %>

<p><%= link_to "Страница дороги #{@company.code}", company_path(@company) %></p>
