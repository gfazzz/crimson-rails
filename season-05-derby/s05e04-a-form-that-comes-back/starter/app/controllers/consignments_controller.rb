# Бланки из Дерби — прямо в реестр.
#
# У формы два исхода, и у каждого свой код. Turbo на этих кодах держится.
class ConsignmentsController < ApplicationController
  before_action :set_roads, only: %i[new create]

  # Пустой бланк.
  #
  # TODO: если пришли со страницы дороги (`?company=MID`), дорога-отправитель
  #       уже выбрана.
  def new
    @consignment = Consignment.new
  end

  # TODO: create — принять бланк.
  #
  #       Принято — 303 на страницу дороги-отправителя и сообщение, что бланк
  #       внесён. Не принято — 422 и та же форма, с тем, что вписали, и с
  #       причинами.
  #
  #       И третий исход, которого не видно, пока не случится: валидация
  #       посмотрела и не нашла, а между её взглядом и записью бланк с тем же
  #       номером успел лечь. Отказал индекс (s04e04). Этот отказ тоже обязан
  #       дойти до кассира словами — не страницей 500.

  private

  # TODO: что форма вправе прислать — и больше ничего. Отметку о расчёте
  #       ставит счёт за месяц, а не тот, кто вносит бланк.
  #
  #       Запрос, в котором бланка нет вовсе, — не непринятый бланк, а
  #       неправильный запрос: 400.

  def set_roads
    @roads = Company.order(:code)
  end
end
