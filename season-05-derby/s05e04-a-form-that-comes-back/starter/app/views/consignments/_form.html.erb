<%# Форма — обещание контроллеру: какие поля в ней есть, то он и получит. %>
<%= form_with model: consignment do |form| %>
  <%# TODO: если бланк не принят — причины словами модели, над формой, так,
            чтобы читающий вслух услышал их первыми (s03e04). %>

  <%# TODO: поля бланка, у каждого — подпись:
              дорога-отправитель — выбором из `roads`, по коду;
              номер бланка, описание груза — строками;
              дата отправки — датой;
              плата в пенсах и вес в фунтах — целыми числами больше нуля.

            Подписи Rails возьмёт из config/locales/ru.yml сам: `form.label`
            спрашивает у модели, как поле зовётся по-русски. %>

  <p><%= form.submit "Внести бланк" %></p>
<% end %>
