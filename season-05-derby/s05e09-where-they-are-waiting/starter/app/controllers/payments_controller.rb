# Выплата по расчёту.
#
# Отвечает двумя способами, по тому, что спрашивающий готов принять: Turbo
# просит поток первым; кто потока не понимает — получает то, что получал бы
# всегда.
class PaymentsController < ApplicationController
  # TODO: расчёт — в пределах дороги, как бланк в s05e06.

  # TODO: create — оплатить (`pay!` из s04e09: под блокировкой и ровно один
  #       раз).
  #
  #       Turbo — поток: payments/create.turbo_stream.erb.
  #       Без Turbo — 303 на лист расчётов и сообщение.
  #
  #       А если расчёт уже оплачен — из соседнего окна, минуту назад? Страница,
  #       с которой нажали, устарела. Отказ — 422, и поток приносит на
  #       страницу правду: строку в том виде, в каком она есть, и причину
  #       (payments/refused.turbo_stream.erb). Без Turbo — лист расчётов, 422,
  #       причина.
end
