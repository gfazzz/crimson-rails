<%# TODO: у строки — id, по которому поток её заменит (`dom_id`). %>
<tr>
  <th scope="row"><%= settlement.period %></th>
  <td><%= money(settlement.pence) %></td>
  <td><%= settlement_state(settlement) %></td>
  <%# TODO: клетка «Оплата»: у неоплаченного расчёта — кнопка «Оплатить»,
            которая шлёт выплату на адрес этого расчёта. У оплаченного —
            пустая клетка: клеток в строке столько, сколько столбцов. %>
</tr>
