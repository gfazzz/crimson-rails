<%# TODO: три действия, три цели — и каждая цель есть на листе расчётов:
            заменить строку расчёта (тем же частичным шаблоном, что в таблице);
            дописать запись в журнал подтверждений: месяц и сумма;
            сказать в живую область, что расчёт оплачен. %>
