<% content_for :title, "Расчёты #{@company.code}" %>

<h1>Расчёты с дорогой <%= @company.code %></h1>

<%# TODO: таблица расчётов (она была на странице дороги с s05e03) — внутри
          фрейма с тем же id, что у фрейма на странице дороги. На запрос
          фрейма Turbo возьмёт из этого ответа только его; на обычный запрос
          — вся страница вокруг. %>

<p><%= link_to "Страница дороги #{@company.code}", company_path(@company) %></p>
