# Помощники страниц с расчётами.
module SettlementsHelper
  # TODO: settlement_state(settlement) — состояние словом: «посчитан»,
  #       «оплачен». Имена `pending` и `paid` — для кода; вслух их не читают.
end
