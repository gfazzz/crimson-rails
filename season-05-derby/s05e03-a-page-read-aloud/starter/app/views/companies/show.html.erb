<% content_for :title, @company.code %>

<h1><%= @company.code %></h1>

<p><%= @company.name %></p>

<% if @company.registered_on %>
  <p>В реестре с <%= @company.registered_on %>.</p>
<% end %>

<%# TODO: расчёты с дорогой — таблицей, если они есть.

          Подпись — о чём таблица. Заголовки столбцов — месяц, сумма,
          состояние, каждый объявлен заголовком столбца. Строка — в
          частичном шаблоне settlements/_settlement, и названа своим месяцем.

          Нет расчётов — фраза, а не пустая таблица. %>

<p><%= link_to "Весь реестр", companies_path %></p>
