<% content_for :title, "Правка #{@company.code}" %>

<h1>Правка дороги <%= @company.code %></h1>

<%# TODO: форма правки. Существующую запись `form_with` сам отправит на её
          адрес глаголом PATCH.

          Причины отказа — над формой, словами модели, `role="alert"`.
          Код показан, но не правится: это адрес дороги.
          Поля — название и дата вступления в реестр. %>

<p><%= link_to "Отмена", company_path(@company) %></p>
