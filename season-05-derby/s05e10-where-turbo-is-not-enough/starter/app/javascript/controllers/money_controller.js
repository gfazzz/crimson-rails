import { Controller } from "@hotwired/stimulus"

// Сумма фунтами, шиллингами и пенсами — пока кассир набирает пенсы.
//
// Это то место, где Turbo мало: спрашивать сервер на каждую нажатую клавишу —
// абсурд, а ответ на вопрос «сколько это в фунтах» не требует ничего, кроме
// числа в поле. И поэтому же — только показ: правда о сумме у сервера.
export default class extends Controller {
  // TODO: цели — поле пенсов и место, где читается сумма.

  // TODO: значения — сколько пенсов в шиллинге и шиллингов в фунте. Они
  //       приходят из разметки, от помощника на сервере (s05e03): числа не
  //       пишутся здесь второй раз.

  // TODO: при подключении — сразу показать чтение того, что уже в поле.
  //       Подумай, когда элемент «подключается»: при первой загрузке — и когда
  //       ещё? (s05e07)

  // TODO: show — прочесть поле и показать «£3 4s 7d». Пустое, отрицательное,
  //       нецелое — не сумма: прочерк, а не «NaN».
}
