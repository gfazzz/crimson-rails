# frozen_string_literal: true

# s01e07 — Символ и строка
#
#   cp starter.rb artifacts/keys.rb
#   make test

module Abacus
  # Приведение ключей.
  #
  # Японская контора пишет ключи латиницей — строками. Палата пишет символами.
  # Для Ruby это разные ключи, и две одинаковые с виду записи одинаковыми
  # не являются.
  module Keys
    # Рекурсивно привести ключи хешей к символам.
    # Значения не трогаются: строка-значение остаётся строкой.
    # TODO: рекурсивно — вложенные хеши и хеши внутри массивов тоже.
    # TODO: значения не трогать: строка-значение остаётся строкой.
    # TODO: ключ, который не умеет to_sym (число, например), оставить как есть.
    # TODO: исходный хеш не менять — вернуть новый.
    def self.symbolize(value)
    end

    # Обратно — в строки: так ведомость отправляют в японскую контору.
    # TODO: то же самое в обратную сторону.
    def self.stringify(value)
    end

    # Две записи об одной перевозке, даже если ключи набраны по-разному.
    # TODO: одна строка.
    def self.same_row?(first, second)
    end

    # Убрать записи, отличающиеся только видом ключей.
    # Остаётся первая встретившаяся, в исходном виде и на исходном месте.
    # TODO: остаётся ПЕРВАЯ встретившаяся запись, в исходном виде
    #       (ключи не переписываются) и на исходном месте.
    # TODO: uniq здесь не поможет — подумай почему, прежде чем писать.
    def self.dedupe(rows)
    end
  end
end
