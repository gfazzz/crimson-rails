# frozen_string_literal: true

# s01e09 — Примесь
#
#   cp starter.rb artifacts/convertible.rb
#   make test

module Abacus
  # Пересчёт веса между единицами.
  #
  # Подмешивается в любой класс, у которого есть weight и unit. Модуль не знает,
  # как эти методы устроены, и знать не должен: он описывает роль, а не
  # устройство.
  module Convertible
    # Сколько килограммов в одной единице.
    IN_KG = { kg: 1.0, kan: 3.75, lb: 0.45359237 }.freeze

    # Хук: Ruby зовёт его в момент include, передавая класс-хозяина.
    #
    # TODO: сделать так, чтобы у класса-хозяина появился метод КЛАССА units,
    #       возвращающий известные единицы. include сам методов класса
    #       не добавляет — нужен второй способ подмешивания.
    def self.included(base)
    end

    module ClassMethods
      # TODO: известные единицы.
      def units
      end
    end

    # Вес в килограммах. Опирается на weight и unit класса-хозяина.
    #
    # TODO: модуль не знает, как устроен хозяин, — он просто зовёт его методы.
    def weight_kg
    end

    # Вес в запрошенной единице.
    #
    # TODO: через килограммы, а не таблицей всех пар единиц.
    def weight_in(target)
    end

    # TODO: неизвестная единица — ArgumentError со списком известных.
    def self.factor_for(unit)
    end
  end

  # Округление веса до целых килограммов.
  #
  # Подмешивается через prepend: модуль встаёт ПЕРЕД классом в цепочке, и
  # super из него зовёт исходный метод. Через include так не получится —
  # метод класса перекрыл бы метод модуля.
  module Rounded
    # TODO: округлить то, что вернул исходный weight_kg.
    # TODO: как получить исходный, если этот метод его перекрывает?
    def weight_kg
    end
  end
end
