// address.js — вид журнала и его адрес.
//
// Состояние журнала уже лежит в одном месте (s03e05), приходит из конторы
// (s03e07) и показывается своими элементами (s03e09). Осталось одно: часть
// этого состояния — не наша.
//
// Отбор — это не то, что читатель выбрал в этой вкладке. Это то, на что он
// сейчас смотрит, и у этого есть имя снаружи: адрес страницы. Пока отбор живёт
// только в памяти вкладки, ссылка не работает, «назад» не работает,
// перезагрузка теряет вид, а тот же вид у соседа собрать невозможно.
//
//     вид, который нельзя назвать адресом, нельзя ни показать другому,
//     ни вернуть себе.
//
// И второе, про доверие: адрес пишет кто угодно. Ссылку присылают. Всё, что
// пришло из адреса, — чужой текст (s03e08).

// TODO: view({ filter }) — вид журнала. Поле пока одно, и всё равно это
//       отдельная вещь. Отбор — только из списка разрешённого; неизвестное
//       значение не ошибка, а просто «не отбор».

// TODO: readAddress(search) — что сказано в адресе. Разбирать URLSearchParams,
//       а не строку руками.

// TODO: addressOf(shown, here) — адрес вида. Пустой отбор вопроса не
//       оставляет: «все записи» — это та же страница. Чужие части адреса
//       (другие параметры, якорь) не теряются: адрес принадлежит не только нам.

// TODO: showAddress(shown, { replace }) — записать вид в историю.
//         pushState     новый след: «назад» вернёт прошлый вид;
//         replaceState  тот же след: привести адрес в порядок.
//       Адрес, совпадающий с нынешним, не писать вовсе: иначе одно и то же
//       нажатие копит историю из одинаковых видов.

// TODO: onAddress(handler) — читатель нажал «назад». Вид брать из адреса, а
//       не из history.state: у первой записи состояние null всегда, а адрес
//       есть всегда. Вернуть функцию отписки.
