// telegraph.js — улучшение журнала приёма.
//
// Редакция вторая: сводка смены и пометка записей без почерка.
//
// Правила первой редакции в силе: ничего не удалять, ничего не отнимать у
// разметки, переживать повторное выполнение. К ним добавляется третье:
// **страница — дерево, а не строка.**
//
// TODO: собрать сводку перед журналом: <ul id="tally"> с четырьмя строками,
//       каждая помечена своим data-tally: sent, held, failed, hand
// TODO: числа считать по data-state записей, а не вписывать
// TODO: «последняя принята» — почерк последней записи, у которой он есть
// TODO: записи без почерка пометить data-unsigned и видимым текстом
// TODO: пометку снимать, когда почерк появился
// TODO: ни одного innerHTML: узлы создаются и находятся, а не собираются
//       строкой. Присваивание innerHTML списку пересоздаёт все записи
//
// Полное ТЗ — в mission.md.

document.documentElement.dataset.script = "on";

// То, что нужно только без скрипта. Кнопка «Показать» отправляет фильтр на
// сервер; со скриптом фильтр отработает на месте (s03e03), и кнопка станет
// лишней. Она прячется, а не удаляется.
for (const node of document.querySelectorAll("[data-without-script]")) {
  node.hidden = true;
}
