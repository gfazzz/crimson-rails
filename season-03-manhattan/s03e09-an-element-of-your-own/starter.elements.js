// elements.js — свои элементы.
//
// Свой элемент — это поведение, привязанное к тегу, а не к строке кода,
// нашедшей этот тег. Разница видна ровно там, где тег появился позже модуля:
// строку журнала, пришедшую из конторы через час после загрузки, никто не
// «подключает» — она поднимается сама.
//
// Три правила жизненного цикла, из которых следует всё остальное:
//   конструктор   элемент ещё не в документе: детей нет, атрибутов может не
//                 быть, трогать нечего. Здесь только своё, внутреннее;
//   connected     поставили на страницу: завести таймер, подписаться;
//   disconnected  сняли: погасить таймер, отписаться. Обязательно.

// TODO: sinceWords(at, now) — сколько прошло, словами:
//         моложе минуты  → «только что»
//         моложе часа    → «N минут назад»
//         моложе суток   → «N часов назад»
//         иначе          → «N дней назад»
//       Склонения берёт Intl.RelativeTimeFormat("ru"), сочинять их не нужно.

// TODO: class TimeAgo extends HTMLElement — живое «сколько прошло».
//         · observedAttributes: о смене чего просить сообщать;
//         · connectedCallback: запомнить запасное содержимое (оно написано в
//           разметке) и посчитать;
//         · attributeChangedCallback: пересчитать — но только если элемент
//           в документе; вне документа показывать некому;
//         · disconnectedCallback: погасить таймер. Без этой строки снятая
//           запись продолжает тикать в памяти: её не видно, и она работает;
//         · без datetime — вернуть запасное содержимое и ничего не считать;
//         · точное время оставить в title;
//         · шаг обновления зависит от возраста: запись минутной давности
//           меняется каждые несколько секунд, трёхдневная — уже никогда.

// TODO: class ShiftNotice extends HTMLElement — живая область смены.
//         · say(text) — положить текст текстом и показать;
//         · clear() — опустошить и спрятать;
//         · фокус не забирать: область сообщает, а идти к ней или нет,
//           решает читатель.

// TODO: customElements.define(...) для обоих тегов. Имя своего тега обязано
//       содержать дефис — так браузер отличает наши теги от тех, которые он
//       однажды добавит сам.
