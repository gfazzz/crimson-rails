// office.js — разговор с конторой.
//
// Подделка кончилась: здесь настоящий запрос по проводу. Ничего, кроме fetch,
// для этого не нужно — ни библиотеки, ни обёртки. Файл wire.js из прошлой
// серии больше не нужен: его место занимает контора.
//
// Три вещи, которых у подделки не было и которые начинаются ровно здесь:
//   код ответа    контора отвечает числом, и число значит разное;
//   отмена        запрос можно оборвать, а не только перестать ждать;
//   повтор        отправить дважды — не то же, что завести две записи.
//
// TODO: класс OfficeError — со status, field (какое поле не понравилось) и
//       retriable (имеет ли смысл повторять)
// TODO: send(entry, { origin, signal, receipt }) — POST /entries телом JSON:
//         201 → вернуть заведённую конторой запись с её номером
//         422 → OfficeError со status и field; повторять бессмысленно
//         5xx → OfficeError, retriable: true
//         обрыв провода → OfficeError с cause, retriable: true
//         отмена → AbortError как есть, не переодевая
//       Ключ приёма (receipt) уезжает заголовком x-receipt. Только ASCII:
//       кириллица в значении заголовка — это ошибка ещё до отправки
// TODO: load({ origin, state, signal }) — GET /entries, отбор параметром
// TODO: withRetry(work, { times, pause }) — повторять только retriable,
//       с выдержкой и с тем же ключом приёма
//
// Полное ТЗ — в mission.md.
