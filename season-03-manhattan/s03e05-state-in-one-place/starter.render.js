// render.js — как состояние выглядит на странице.
//
// Одна функция и одно правило: **отрисовка приводит дерево к состоянию.**
// Не дописывает к прошлому, не помнит, что делала в прошлый раз, и не
// спрашивает у дерева, что там сейчас.
//
// Отсюда два свойства, которые проверяются: отрисовать дважды — то же, что
// один раз; испортить дерево руками и отрисовать — вернуться к состоянию.
//
// TODO: render(root, state) — сводка и записи
// TODO: сводка: по строке на состояние плюс строка про последний почерк
// TODO: записи: data-state, hidden по отбору, data-chalk, data-unsigned
//       и видимая пометка «почерка нет»
// TODO: записи, которой нет на странице, но есть в состоянии, — создать
// TODO: записи не пересоздавать: их находят по data-id (s03e02)
//
// Полное ТЗ — в mission.md.

import { counts, lastHand, shown } from "./state.js";
