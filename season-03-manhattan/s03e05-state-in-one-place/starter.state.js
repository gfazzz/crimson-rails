// state.js — состояние журнала и всё, что с ним делают.
//
// Здесь не должно быть ни одного обращения к странице. Это не аккуратность, а
// условие: пока состояние знает про дерево документа, проверить его можно
// только открыв страницу.
//
// Все функции ниже **не меняют** то, что им дали: они возвращают новое
// состояние. Пока прежнее цело, есть куда вернуться.
//
// TODO: register({ entries, filter }) — собрать состояние
// TODO: entry({ id, state, hand, text, chalk }) — одна запись
// TODO: read(root) — прочитать начальное состояние из разметки (единственная
//       функция, которой отдают узел, и она только читает)
// TODO: withFilter(state, filter) — новый отбор
// TODO: withChalk(state, id) — переключить мел у записи
// TODO: withEntry(state, fresh) — добавить запись в конец
// TODO: shown(state) — какие записи показывать при нынешнем отборе
// TODO: counts(state) — сколько записей в каждом состоянии
// TODO: lastHand(state) — почерк последней записи, у которой он есть
//
// Полное ТЗ — в mission.md.
